import type { Meta, StoryObj } from '@storybook/react-vite'; import { Button } from '../Button'; import { Result } from './Result';
const meta = { title: 'Feedback/Result', component: Result, tags: ['autodocs'], args: { status: 'success', title: 'Payment complete', description: 'Your receipt has been sent by email.', actions: <Button>Back to home</Button> } } satisfies Meta<typeof Result>; export default meta; type Story = StoryObj<typeof meta>; export const Success: Story = {}; export const Error: Story = { args: { status: 'error', title: 'Something went wrong', description: 'Try again in a moment.' } };
