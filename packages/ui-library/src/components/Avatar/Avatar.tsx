import { useState, type HTMLAttributes, type ReactNode } from 'react';
export interface AvatarProps extends HTMLAttributes<HTMLSpanElement> { src?: string; alt?: string; children?: ReactNode; size?: 'small' | 'medium' | 'large' | number; shape?: 'circle' | 'square' }
export function Avatar({ src, alt = '', children, size = 'medium', shape = 'circle', className = '', style, ...props }: AvatarProps) { const [failed, setFailed] = useState(false); const px = typeof size === 'number' ? size : { small: 28, medium: 40, large: 56 }[size]; return <span className={`ui-avatar ui-avatar--${shape}${className ? ` ${className}` : ''}`} style={{ width: px, height: px, ...style }} {...props}>{src && !failed ? <img src={src} alt={alt} onError={() => setFailed(true)} /> : children}</span>; }
