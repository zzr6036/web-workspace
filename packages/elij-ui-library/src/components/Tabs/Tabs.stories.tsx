import type { Meta, StoryObj } from '@storybook/react-vite'; import { Tabs } from './Tabs';
const items = [{ key: 'overview', label: 'Overview', children: 'Project overview content.' }, { key: 'activity', label: 'Activity', children: 'Recent activity content.' }, { key: 'settings', label: 'Settings', children: 'Settings content.' }]; const meta = { title: 'Navigation/Tabs', component: Tabs, tags: ['autodocs'], args: { items } } satisfies Meta<typeof Tabs>; export default meta; type Story = StoryObj<typeof meta>; export const Horizontal: Story = {}; export const Vertical: Story = { args: { orientation: 'vertical' } };
