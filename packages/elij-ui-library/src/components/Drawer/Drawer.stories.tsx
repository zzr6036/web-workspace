import { useState } from 'react'; import type { Meta, StoryObj } from '@storybook/react-vite'; import { useUI } from '../../provider'; import { Button } from '../Button'; import { Drawer, type DrawerProps } from './Drawer';
function Demo(args: DrawerProps) { const { t } = useUI(); const [open, setOpen] = useState(false); return <><Button onClick={() => setOpen(true)}>{t('open')}</Button><Drawer {...args} open={open} onOpenChange={setOpen} footer={<Button onClick={() => setOpen(false)}>{t('confirm')}</Button>}/></>; } const meta = { title: 'Feedback/Drawer', component: Drawer, tags: ['autodocs'], args: { open: false, onOpenChange: () => undefined, title: 'Settings', children: 'Drawer content belongs here.' } } satisfies Meta<typeof Drawer>; export default meta; type Story = StoryObj<typeof meta>; export const Right: Story = { render: (args) => <Demo {...args} /> }; export const Left: Story = { args: { placement: 'left' }, render: (args) => <Demo {...args} /> };
