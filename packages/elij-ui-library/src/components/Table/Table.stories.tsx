import type { Meta, StoryObj } from '@storybook/react-vite'; import { useUI } from '../../provider'; import { Badge } from '../Badge'; import { Table } from './Table';
type User = { id: number; name: string; role: string; status: string }; function Demo({ empty = false }: { empty?: boolean }) { const { locale } = useUI(); const zh = locale === 'zh-CN'; const columns = [{ key: 'name', title: zh ? '姓名' : 'Name', dataIndex: 'name' as const }, { key: 'role', title: zh ? '角色' : 'Role', dataIndex: 'role' as const }, { key: 'status', title: zh ? '状态' : 'Status', dataIndex: 'status' as const, render: (value: unknown) => <Badge variant="success">{String(value)}</Badge> }]; const data: User[] = [{ id: 1, name: 'Alex Chen', role: zh ? '设计师' : 'Designer', status: zh ? '启用' : 'Active' }, { id: 2, name: 'Sam Lee', role: zh ? '工程师' : 'Engineer', status: zh ? '启用' : 'Active' }]; return <Table<User> columns={columns} data={empty ? [] : data} rowKey="id" caption={zh ? '团队成员' : 'Team members'} />; } const meta = { title: 'Data Display/Table', component: Table, tags: ['autodocs'] } satisfies Meta<typeof Table>; export default meta; type Story = StoryObj; export const Default: Story = { render: () => <Demo /> }; export const Empty: Story = { render: () => <Demo empty /> };
